# Optional — not used by the current HTTP-only handoff.
# Compose publishes the app directly on host TCP 80 (no Caddy, no 443).
# Keep this file only if you later restore an HTTPS edge with certs/.

{
	admin off
	auto_https off
}

:80 {
	encode gzip
	reverse_proxy iboard:3001
}
